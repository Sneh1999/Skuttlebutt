diff --git a/index.js b/index.js
index d868233..52e29c7 100644
--- a/index.js
+++ b/index.js
@@ -92,9 +92,9 @@ exports.checkInvalidCheap = function (state, msg) {
     //most likely, we just tried to append two messages twice
     //or append another message after an error.
     if(msg.sequence != state.sequence + 1)
-      return nonFatalError('invalid message: expected sequence ' + (state.sequence + 1) + ' but got:'+ msg.sequence + 'in state:'+JSON.stringify(state)+', on feed:'+msg.author)
+      return fatalError('invalid message: expected sequence ' + (state.sequence + 1) + ' but got:'+ msg.sequence + 'in state:'+JSON.stringify(state)+', on feed:'+msg.author)
     if(isNaN(state.timestamp))
-      return nonFatalError('state must have timestamp property, on feed:'+msg.author)
+      return fatalError('state must have timestamp property, on feed:'+msg.author)
     //if the timestamp doesn't increase, they should have noticed at their end.
     if(msg.timestamp <= state.timestamp)
       return nonFatalError('invalid message: timestamp not increasing, on feed:'+msg.author)
@@ -127,7 +127,7 @@ exports.checkInvalid = function (state, hmac_key, msg) {
 {
   //an array of messages which have been validated, but not written to the database yet.
   valid: [],
-  //a map of information needed to know if something should be appeneded to the valid queue.
+  //a map of information needed to know if something should be appended to the valid queue.
   feeds: {
     <feed>: {id, sequence, ts}
   },
@@ -161,9 +161,9 @@ function flatState (fstate) {
 }
 
 exports.append = function (state, hmac_key, msg) {
-  var err
   var _state = flatState(state.feeds[msg.author])
-  if(err = exports.checkInvalid(_state, hmac_key, msg))
+  var err = exports.checkInvalid(_state, hmac_key, msg)
+  if(err)
     throw err
 
   else if(state.feeds[msg.author]) {
